# Nixpacks configuration for Codebuff deployment
# This configures the build environment for Railway

[phases.setup]
nixPkgs = ["nodejs_20", "bun", "openssl"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = [
  "bun --cwd web run build"
]

[start]
cmd = "node web/test-server.js"
